name: Update Jadwal Wayang

on:
workflow_dispatch:
schedule:
- cron: '0 */3 * * *'

jobs:
update:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout
    uses: actions/checkout@v3

  - name: Download RSS
    run: |
      curl -L "https://www.kluban.net/feeds/posts/default?alt=json" > rss.json

  - name: Push File
    run: |
      git config --global user.name "github-actions"
      git config --global user.email "actions@github.com"
      git add rss.json
      git commit -m "Update RSS" || echo "No changes"
      git push
